# OCR Workflow v2

A snapshot/revision of the [OCR Workflow](../ocr-workflow/) with the same Mistral-based
pipeline. See the main OCR workflow README for full documentation.

## Differences from v1

This was exported as a point-in-time backup (`Dec 3 at 23:40:51`). The node structure
and Mistral API calls are identical to v1. Check the `workflow.json` in both directories
and use whichever has the most recent updates — the active instance in n8n is the source
of truth.

## See Also

- [ocr-workflow/README.md](../ocr-workflow/README.md) — full documentation
